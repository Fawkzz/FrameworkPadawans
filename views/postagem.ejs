<%- include("./partials/header") %>


<main>
    <div class="container" id="table">
        <h5>Listagem das Postagem</h5>
        <div class="card">
            <table id="tabela" class="table table-striped" style="width:100%">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>TITULO</th>
                    <th>POSTAGEM</th>
                </tr>
                </thead>

                <tbody>
                    <% if (typeof apiResponse != 'undefined') { %>                        
                   
                    <% apiResponse.forEach((post) => { %>
                        <tr>
                            <td><%= post.id %></td>
                            <td><%= post.title %></td>
                            <td><%= post.body %></td>
                         </tr>
                       
                    <% }); %>
                    <% } %>
                 </tbody>
                <tfoot>
                    <tr>
                        <th>ID</th>
                        <th>TITULO</th>
                        <th>POSTAGEM</th>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</main>
<%- include("./partials/footer") %>